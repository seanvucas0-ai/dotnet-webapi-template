on:
  workflow_dispatch:
    inputs:
      service_name:
        description: "Service Name"
        required: true
        type: string

      description:
        description: "Service Description"
        required: true
        type: string

      team_name:
        description: "Team Name"
        required: true
        type: string
